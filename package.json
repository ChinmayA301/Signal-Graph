{
  "name": "signalgraph",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared-types"
  ],
  "scripts": {
    "web:dev": "npm run dev -w @signalgraph/web",
    "web:build": "npm run build -w @signalgraph/web",
    "types:build": "npm run build -w @signalgraph/shared-types"
  }
}
